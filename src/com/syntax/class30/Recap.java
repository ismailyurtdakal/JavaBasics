package com.syntax.class30;
public class Recap {

/*Map- is an object that stores group of objects key+value=entry
 * Values in Map can be duplicated but keys always must be unique
 * 
 * Map is an Interface
 * 
 * Map has 4 implementation class(HashMap, LinkedHashMap, TreeMap 
 * and HashTable(very similar to HashMap
* 	
* to add values into Map--> .put<K,V>
* to get a value from a map--> map.get(key);
* to remove the key/value--> map.remove(key);
* to replace a values of the key--> map.replace(key);
* to verify is key/values exist in the map--> map.contains(key); / map.contains(value);

* 
* 1).values(); gives all values  ==> Collection of values
* 2).keySet(); gives all keys  ==> set of Keys
* 3).entrySet(); gives all entries
* 
* 4) Entyr<K,V>  --> getValue and getKey will be available
*
* 
* 
* 
* 
* 
*/

	}


